exports = module.exports = new <%= name.slice(0,1).toUpperCase() + name.slice(1) %>

function <%= name.slice(0,1).toUpperCase() + name.slice(1) %>() {
  this.commands = [];
  this.options = [];
  this.args = [];
}
